# Env for TrainTicket's own docker-compose.yml. Upstream's .env sets IMG_REPO/IMG_TAG only;
# ts-avatar-service uses ${NAMESPACE}/ts-avatar-service:${TAG}, which upstream leaves undefined.
IMG_REPO=codewisdom
IMG_TAG=0.2.0
NAMESPACE=codewisdom
TAG=0.2.0
