# Generated by gen-override.py from TrainTicket's docker-compose.yml. Do not edit.
# See the docstring in gen-override.py for what each addition is for.
services:
  redis:
    healthcheck:
      test:
      - CMD-SHELL
      - redis-cli ping | grep -q PONG
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
  ts-ui-dashboard:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS -o /dev/null http://127.0.0.1:8080/ || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-auth-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12340/api/v1/auth/hello
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-auth-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-auth-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-user-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12342/api/v1/userservice/users/hello
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-user-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-user-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-verification-code-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:15678/ | grep
        -qE '^(200|403|404)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-account-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-account-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-route-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:11178/api/v1/routeservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-route-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-route-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-contacts-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12347/ | grep
        -qE '^(200|403|404)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-contacts-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-contacts-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-order-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12031/api/v1/orderservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-order-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-order-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-order-other-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12032/api/v1/orderOtherService/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-order-other-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-order-other-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-config-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:15679/api/v1/configservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-config-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-config-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-station-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12345/api/v1/stationservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-station-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-station-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-train-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:14567/ | grep
        -qE '^(200|403|404)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-train-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-train-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-travel-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12346/api/v1/travelservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-travel-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-travel-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-travel2-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16346/api/v1/travel2service/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-travel2-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-travel2-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-preserve-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:14568/api/v1/preserveservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-preserve-other-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:14569/api/v1/preserveotherservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-basic-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:15680/api/v1/basicservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-ticketinfo-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:15681/api/v1/ticketinfoservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-price-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16579/ | grep
        -qE '^(200|403|404)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-price-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-price-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-notification-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:17853/api/v1/notifyservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-security-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:11188/api/v1/securityservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-security-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-security-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-inside-payment-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18673/api/v1/inside_pay_service/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-inside-payment-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-inside-payment-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-execute-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:12386/ | grep
        -qE '^(200|403|404)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-payment-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:19001/api/v1/paymentservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-payment-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-payment-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-rebook-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18886/api/v1/rebookservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-rebook-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-rebook-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-cancel-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18885/api/v1/cancelservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-assurance-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18888/api/v1/assuranceservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-assurance-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-assurance-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-seat-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18898/api/v1/seatservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-travel-plan-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:14322/api/v1/travelplanservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-ticket-office-service:
    healthcheck:
      test:
      - CMD-SHELL
      - bash -c 'exec 3<>/dev/tcp/127.0.0.1/16108' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-ticket-office-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-ticket-office-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-news-service:
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO /dev/null http://127.0.0.1:12862/ || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-news-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-news-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-voucher-mysql:
    image: mysql:5.7
    volumes:
    - /tank/tt-voucher-mysql:/var/lib/mysql
    healthcheck:
      test:
      - CMD-SHELL
      - mysqladmin ping -uroot -proot --silent
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s
  ts-voucher-service:
    healthcheck:
      test:
      - CMD-SHELL
      - bash -c 'exec 3<>/dev/tcp/127.0.0.1/16101' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-food-map-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18855/ | grep
        -qE '^(200|403|404)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-food-map-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-food-map-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-route-plan-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:14578/api/v1/routeplanservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-food-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18856/api/v1/foodservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-consign-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16111/api/v1/consignservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-consign-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-consign-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-consign-price-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16110/api/v1/consignpriceservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-consign-price-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-consign-price-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-food-mongo:
    image: mongo:4.4
    volumes:
    - /tank/tt-food-mongo:/data/db
    healthcheck:
      test:
      - CMD-SHELL
      - mongo --quiet --eval 'db.runCommand({ping:1}).ok' | grep -q 1
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
  ts-admin-basic-info-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:18767/api/v1/adminbasicservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-admin-order-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16112/api/v1/adminorderservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-admin-route-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16113/api/v1/adminrouteservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-admin-travel-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16114/api/v1/admintravelservice/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-admin-user-service:
    healthcheck:
      test:
      - CMD-SHELL
      - curl -s -o /dev/null -m 4 -w '%{http_code}' http://127.0.0.1:16115/api/v1/adminuserservice/users/welcome
        | grep -qE '^(200|403)$' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
  ts-avatar-service:
    healthcheck:
      test:
      - CMD-SHELL
      - bash -c 'exec 3<>/dev/tcp/127.0.0.1/17001' || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 150s
